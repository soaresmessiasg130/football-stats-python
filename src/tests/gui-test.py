import tkinter as tk

window = tk.Tk()

window.title("Football Stats")
window.geometry("600x400")

label = tk.Label(text = "Championships")

label.grid(column = 0, row = 0)

label2 = tk.Label(text = "KSAJLKAJSLSAJKA")

label2.grid(column = 0, row = 0)

window.mainloop()
